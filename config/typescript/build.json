{
    "$schema": "https://json.schemastore.org/tsconfig",
    "compilerOptions": {
        "tsBuildInfoFile": "${configDir}/.build/tsconfig.build.tsbuildinfo",
        "outDir": "${configDir}/.build/dist",
        "rootDir": "${configDir}/src",
        "noEmit": false
    },
    "include": ["${configDir}/src/"]
}
