{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": ["./base.json"],
    "exclude": [
        "${configDir}/node_modules",
        "${configDir}/build",
        "${configDir}/test",
        "${configDir}/docs"
    ]
}
