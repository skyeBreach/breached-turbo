# Use this to build commit messages
prepare-commit-msg:
    commands:
        commitzen:
            interactive: true
            run: pnpm commit
            env:
                LEFTHOOK: 0

# Use this to validate commit messages
commit-msg:
    scripts:
        "commitlint.sh":
            runner: bash
